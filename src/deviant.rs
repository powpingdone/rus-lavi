// deviant.rs: the main logic for finding the least average image

pub fn least_average_arr<'a>(imgs: &'a Vec<String>, res: &'a(u32, u32), largest: &'a String) -> Vec<(u8, u8, u8)> {
    Vec::new()
}
